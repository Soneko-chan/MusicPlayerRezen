<Page x:Class="UI.PlaylistPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:helpers="clr-namespace:UI.Helpers"
      Title="PlaylistPage">
    <Page.Resources>
        <helpers:PlaylistCoverConverter x:Key="PlaylistCoverConverter"/>
        <helpers:DurationConverter x:Key="DurationConverter"/>
    </Page.Resources>
    <Grid Background="{DynamicResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Информация о плейлисте -->
        <Border Grid.Row="0" Background="{DynamicResource SecondaryColorBrush}" 
                Margin="20,15" CornerRadius="10" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{DynamicResource PrimaryColorBrush}"
                        CornerRadius="8" Width="120" Height="120"
                        ClipToBounds="True">
                    <Image Name="PlaylistCoverImage"
                           Source="{Binding PlaylistCoverPath, Converter={StaticResource PlaylistCoverConverter}}"
                           Stretch="UniformToFill"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" 
                            Margin="15,0">
                    <TextBlock Name="PlaylistNameText" Text="{Binding PlaylistName}"
                               FontSize="28" FontWeight="Bold"
                               Foreground="{DynamicResource TextColorBrush}"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Name="PlaylistTrackCountText" Text="{Binding PlaylistTracks.Count, 
                               StringFormat={}{0} треков}" 
                               FontSize="16" 
                               Foreground="{DynamicResource LightTextColor}" 
                               Margin="0,5,0,0"/>
                    <TextBlock Name="PlaylistDateCreatedText" Text="{Binding DateCreated, 
                               StringFormat=Создан: {0:dd.MM.yyyy}}" 
                               FontSize="14" 
                               Foreground="{DynamicResource LightTextColor}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" VerticalAlignment="Top"
                            HorizontalAlignment="Right">
                    <Button Name="AddTrackButton" Content="Добавить трек"
                            Style="{DynamicResource StandardButtonStyle}"
                            Margin="0,0,0,10" Click="AddTrackButton_Click"/>
                    <Button Name="EditPlaylistButton" Content="Редактировать"
                            Style="{DynamicResource StandardButtonStyle}"
                            Margin="0,0,0,10" Click="EditPlaylistButton_Click"/>
                    <Button Name="DeletePlaylistButton" Content="Удалить плейлист"
                            Style="{DynamicResource DangerButtonStyle}"
                            Click="DeletePlaylistButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Таблица треков -->
        <Grid Grid.Row="1" Margin="20,0,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Название" 
                       FontWeight="Bold" Padding="10,5"
                       Foreground="{DynamicResource LightTextColor}"/>
            <TextBlock Grid.Column="1" Text="Длительность" 
                       FontWeight="Bold" HorizontalAlignment="Right" 
                       Padding="10,5"
                       Foreground="{DynamicResource LightTextColor}"/>
        </Grid>

        <ScrollViewer Grid.Row="2" Margin="15,0">
            <ItemsControl Name="TracksList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource TrackItemStyle}" 
                                Height="60" Margin="5" 
                                MouseLeftButtonUp="TrackItem_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" 
                                        Width="40" Height="40" 
                                        Margin="0,0,10,0" 
                                        VerticalAlignment="Center"
                                        CornerRadius="20" 
                                        ClipToBounds="True">
                                    <Image Source="{Binding CoverPath}" 
                                           Stretch="UniformToFill" />
                                </Border>

                                <StackPanel Grid.Column="1"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextColorBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Artist.ArtistName}"
                                               FontSize="12"
                                               Foreground="{DynamicResource LightTextColor}"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2"
                                           Text="{Binding TrackDuration,
                                           Converter={StaticResource DurationConverter}}"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           Padding="10,0"
                                           Foreground="{DynamicResource TextColorBrush}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
