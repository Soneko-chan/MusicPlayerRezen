<Page x:Class="UI.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:helpers="clr-namespace:UI.Helpers"
      Title="RegisterPage">
    <Grid Background="{DynamicResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" Background="{DynamicResource SecondaryColorBrush}"
                Margin="50" CornerRadius="15" Padding="40" MaxHeight="500">
            <ScrollViewer>
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Width="350">

                    <TextBlock Text="🎵" FontSize="60"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10"
                               Foreground="{DynamicResource TextColorBrush}"/>

                    <TextBlock Text="Регистрация"
                               FontSize="28" FontWeight="Bold"
                               Foreground="{DynamicResource TextColorBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,30"/>

                    <!-- Логин -->
                    <TextBlock Text="Логин" Margin="0,0,0,5"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextColorBrush}"/>
                    <TextBox Name="LoginTextBox"
                             Style="{StaticResource StandardTextBoxStyle}"
                             Margin="0,0,0,15"
                             Padding="15,10"/>

                    <!-- Имя пользователя -->
                    <TextBlock Text="Имя пользователя"
                               Margin="0,0,0,5" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextColorBrush}"/>
                    <TextBox Name="UsernameTextBox"
                             Style="{StaticResource StandardTextBoxStyle}"
                             Margin="0,0,0,15"
                             Padding="15,10"/>

                    <!-- Email -->
                    <TextBlock Text="Email" Margin="0,0,0,5"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextColorBrush}"/>
                    <TextBox Name="EmailTextBox"
                             Style="{StaticResource StandardTextBoxStyle}"
                             Margin="0,0,0,15"
                             Padding="15,10"/>

                    <!-- Пароль -->
                    <TextBlock Text="Пароль" Margin="0,0,0,5"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextColorBrush}"/>
                    <PasswordBox Name="PasswordBox"
                                 helpers:PasswordBoxHelper.Attach="True"
                                 Style="{StaticResource StandardPasswordBoxStyle}"
                                 Margin="0,0,0,15"
                                 Padding="15,10"/>

                    

                    <!-- Подтверждение пароля -->
                    <TextBlock Text="Подтверждение пароля"
                               Margin="0,0,0,5" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextColorBrush}"/>
                    <PasswordBox Name="ConfirmPasswordBox"
                                 helpers:PasswordBoxHelper.Attach="True"
                                 Style="{StaticResource StandardPasswordBoxStyle}"
                                 Margin="0,0,0,20"
                                 Padding="15,10"/>

                    <!-- Кнопка регистрации -->
                    <Button Content="Зарегистрироваться"
                            Name="RegisterButton"
                            Style="{DynamicResource StandardButtonStyle}"
                            HorizontalAlignment="Stretch"
                            Padding="15,12"
                            FontSize="16"
                            Margin="0,0,0,15"
                            Click="RegisterButton_Click"/>

                    <!-- Ссылка на вход -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <TextBlock Text="Уже есть аккаунт? "
                                   Foreground="{DynamicResource TextColorBrush}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="Войти"
                                   Name="LoginLink"
                                   Foreground="{DynamicResource TextColorBrush}"
                                   Cursor="Hand" MouseLeftButtonUp="LoginClick">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="TextDecorations"
                                                    Value="Underline"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
